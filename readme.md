# WP Plugin Downloader

A clean, modern Node.js and Express application that allows users to paste a WordPress plugin URL (e.g., `https://wordpress.org/plugins/jetpack/`) and automatically fetches the latest ZIP version from the official repository.

### Features

⚡️ **Streaming Downloads** — Proxies files directly from the WordPress repository to the client to ensure low memory usage.

🎨 **Modern UI** — Built with Tailwind CSS using a clean Sky Blue and responsive card layout.

🛡️ **Input Validation** — Automatically verifies URL patterns and checks if the plugin exists before attempting a download.

🔔 **Interactive Notifications** — dynamic, dismissible inline alerts for success messages and errors.

🧹 **Smart Form Handling** — Automatically clears input fields upon fetching to ensure a clean workflow for multiple downloads.
